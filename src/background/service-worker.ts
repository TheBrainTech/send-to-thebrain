// Placeholder service worker. Manifest V3 requires a background worker for the
// extension to be considered "active" by some browsers, but all real work in
// this extension happens in the popup. If you later want a keyboard shortcut
// or context-menu trigger, wire that here.

export {};
